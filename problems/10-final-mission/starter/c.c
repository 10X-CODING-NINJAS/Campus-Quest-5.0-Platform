#include <stdio.h>
#include <stdlib.h>
int main() {
    long long n, m, d; scanf("%lld %lld %lld", &n, &m, &d);
    long long x[2001], y[2001];
    for (int i = 0; i < n; i++) scanf("%lld %lld", &x[i], &y[i]);
    // Write your solution here
    return 0;
}
