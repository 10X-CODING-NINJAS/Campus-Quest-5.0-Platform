#include <bits/stdc++.h>
using namespace std;
int main() {
    long long n, k; cin >> n >> k;
    vector<long long> a(n);
    for (auto& x : a) cin >> x;
    // Write your solution here
    return 0;
}
