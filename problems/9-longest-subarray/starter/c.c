#include <stdio.h>
int main() {
    int n; long long k; scanf("%d %lld", &n, &k);
    long long a[100001];
    for (int i = 0; i < n; i++) scanf("%lld", &a[i]);
    // Write your solution here
    return 0;
}
