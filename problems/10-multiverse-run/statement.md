### The Leap of Faith

The final leap! To break through the dimensional barrier, choose two coordinates from the leaps array whose sum equals the leap of faith distance.

Return their indices (0-indexed).

#### Input Format
- First line contains two integers: $N$ and $Target$.
- Second line contains $N$ space-separated integers.

#### Output Format
- Two space-separated integers.
