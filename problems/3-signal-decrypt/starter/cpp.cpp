#include <bits/stdc++.h>
using namespace std;

int main() {
    long long n;
    cin >> n;
    
    // TODO: Print the n-th Fibonacci number (0-indexed: F(0)=0, F(1)=1, F(2)=1, ...)
    cout << 0 << endl;
    return 0;
}
