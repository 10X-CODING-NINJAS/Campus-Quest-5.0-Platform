#include <bits/stdc++.h>
using namespace std;
int main() {
    int n, q; cin >> n >> q;
    vector<int> a(n);
    for (auto& x : a) cin >> x;
    while (q--) {
        int target; cin >> target;
        // Write your binary search here
    }
    return 0;
}
